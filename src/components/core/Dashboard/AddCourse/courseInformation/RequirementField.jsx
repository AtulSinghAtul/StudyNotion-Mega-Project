import React from "react";

const RequirementField = ({
  name,
  label,
  error,
  register,
  setValue,
  getValue,
}) => {
  return <div>RequirementField</div>;
};

export default RequirementField;
